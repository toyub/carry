(function(win, doc, Backbone, $$MIS){
  var MaterialUnitView = Backbone.View.extend({
    events: {
      
    }
  });

  win.$$MIS = $$MIS || {};
  win.$$MIS.MaterialUnitView = MaterialUnitView;
})(window, document, Backbone, window.$$MIS);