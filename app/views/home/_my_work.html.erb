<ul class="work-menu js-work-menu">
  <% @my_works.each do |work| %>
    <li class="item">
      <a href="<%= work[:url]%>">
        <div class="item-icon">
          <i class="fa fa-cubes"></i>
        </div>
        <span class="item-menu"><%= work[:name]%></span>
      </a>
    </li>
  <% end %>
</ul>
