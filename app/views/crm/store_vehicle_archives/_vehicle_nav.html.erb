<ul class="page_card">
  <li class="add_carinfo">
    <%= link_to '+', new_crm_store_customer_store_vehicle_archive_path %>
  </li>
  <% @vehicle_ids.each_with_index do |vehicle_id, index| %>
    <li class='<%= "active" if @vehicle.id == vehicle_id %>' >
      <%= link_to (index+1), crm_store_customer_store_vehicle_archive_path(@customer, vehicle_id) %>
    </li>
  <% end %>
</ul>
